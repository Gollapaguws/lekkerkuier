# Logrotate config for auto-heal script
# Rotates daily, keeps 7 days, compresses old logs

/var/log/auto-heal.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 644 root root
    su root root
}
